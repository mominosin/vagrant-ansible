- hosts: local
  user: vagrant
  sudo: yes
  tasks:
  - name: apt install
    apt:  name={{ item }} update_cache=yes
    with_items:
      - tmux
      - nginx
      - openjdk-7-jdk
      - build-essential
  - name: pip install
    pip:  name={{ item }}
    with_items:
      - fabric
      - awscli
  - name: install scala
    include: scala.yml

  - name: install ruby
    include: ruby.yml

  - name: install gem
    include: gem.yml

  - name: install gem
    include: zsh.yml
